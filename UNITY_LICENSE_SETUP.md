# إعداد رخصة Unity وبناء Apex Arena دون فتح محرر Unity

هذا المستند يشرح المسار الآمن لبناء Apex Arena بصيغة APK أو AAB من خلال GitHub Actions. البناء يتم بمحرك Unity الحقيقي بالإصدار المحدد في `ProjectSettings/ProjectVersion.txt`، ولا يحتاج المستخدم إلى فتح Unity Editor على جهازه.

## المتطلبات

| المتطلب | القيمة أو الوصف |
|---|---|
| إصدار Unity | `2022.3.20f1` |
| منصة البناء | Android |
| المشهد | `Assets/_Project/Scenes/MainArena.unity` |
| سير البناء | `.github/workflows/build.yml` |
| سكربت البناء | `Assets/Editor/BuildPipeline.cs` |
| ناتج البناء | `build/Android/Apex-Arena-debug.apk` |

## مكان الرخصة

لا تُحفظ الرخصة داخل المستودع، ولا داخل `ProjectSettings`، ولا في ملف نصي يُرفع إلى GitHub. يجب تخزينها في GitHub Secrets أو في Unity Cloud Build Secrets. الأسماء التي يتوقعها سير البناء هي:

| الاسم | الاستخدام |
|---|---|
| `UNITY_LICENSE` | محتوى ملف ترخيص Unity أو بيانات الترخيص التي يدعمها GameCI |
| `UNITY_EMAIL` | البريد المرتبط بحساب Unity |
| `UNITY_PASSWORD` | كلمة مرور حساب Unity عند استخدام نمط المصادقة الذي يتطلبها |

لا تضع هذه القيم في رسالة عامة أو في commit. أدخلها فقط من صفحة إعدادات أسرار المستودع، أو استخدم OIDC/ترخيصًا مؤسسيًا إذا كان متوفرًا.

## الحصول على الرخصة

من جهاز يملك Unity Hub وUnity Editor بنفس إصدار المشروع، سجّل الدخول إلى حساب Unity المرخّص، ثم فعّل Unity Personal أو الترخيص المتاح من خلال Unity Hub. بعد ذلك أنشئ ملف ترخيص مخصصًا للبناء الآلي وفق طريقة الترخيص المعتمدة من حسابك وGameCI. إذا كان حسابك يستخدم Unity Personal، تحقّق من شروط Unity الحالية وحدود البناء الآلي قبل إنشاء ملف CI.

لا يمكن استخراج رخصة من حساب المستخدم تلقائيًا دون تسجيل دخول وموافقة صريحة، ولا يمكن إنشاء رخصة صالحة من قيمة تخمينية. لذلك تظل خطوة إدخال الرخصة في GitHub Secrets خطوة يدوية خاصة بمالك الحساب.

## تشغيل البناء

بعد إدخال الأسرار، ادفع تغييرًا إلى الفرع `main` أو شغّل سير `Build Apex Arena` من تبويب Actions. يستخدم سير البناء `game-ci/unity-builder@v4` ويستهدف Android مع `BuildPipeline.BuildAndroid`. بعد نجاح المهمة، نزّل artifact باسم `Apex-Arena-Android`، ثم اختبر APK على جهاز Android قبل أي نشر عام.

## ملاحظات التوقيع والنشر

الناتج الحالي مخصص للتحقق والبناء الأولي، وقد يكون APK غير موقّع بتوقيع متجر الإنتاج. لنشره في Google Play، يجب إنشاء keystore خاص وآمن، وإضافته إلى GitHub Secrets، وضبط إعدادات Android signing في Unity أو في خطوة CI منفصلة. لا ترفع keystore أو كلمة مروره إلى Git.

## حدود التحقق

نجاح إعداد الملفات لا يساوي نجاح البناء. لا يُعد APK جاهزًا للتنزيل إلا بعد ظهور نتيجة Unity CI ناجحة ووجود artifact قابل للتنزيل. كما أن المشروع الحالي يحتوي أنظمة C# ومشهدًا أوليًا، بينما تحتاج الخريطة النهائية والنماذج والحركات والمؤثرات واختبار الشبكة إلى تحقق فعلي داخل Unity أو في بناء CI.
