# خوادم Apex Arena والشبكة

## الحالة الحالية

يعتمد المشروع على **Unity Netcode for GameObjects 1.6.0** و **Unity Transport**. يحتوي `NetworkManager.cs` على مسار Host/Client، عنوان خادم، منفذ افتراضي `7777`، حد أقصى للاعبين، وأحداث الاتصال والانفصال. هذه طبقة اتصال أولية داخل Unity وليست خدمة إنتاج مستضافة بحد ذاتها.

## مكونات الإصدار الإنتاجي

| المكوّن | الدور | الحالة |
|---|---|---|
| Unity game client | اللعب والواجهة والتحكم | موجود في المشروع |
| Netcode for GameObjects | مزامنة كائنات اللعب وRPC | مضاف في Packages |
| Unity Transport | نقل بيانات العميل والخادم | مضاف ومستخدم |
| Dedicated game server | استضافة المباراة دون لاعب محلي | يحتاج تفعيل واختبار Build Target مناسب |
| Lobby/Relay | إنشاء الغرف والاتصال خلف NAT | يحتاج ربط Unity Gaming Services أو بديل موثوق |
| Authentication | هوية اللاعب والجلسات | غير مكتمل في النسخة الحالية |
| Matchmaking | اختيار غرفة وتوزيع اللاعبين | غير مكتمل في النسخة الحالية |
| Persistence service | الحساب والمخزون والتقدم | غير موجود بعد |
| Telemetry/anti-cheat | مراقبة الأعطال والغش | يحتاج تصميمًا واختبارًا مستقلًا |

## قواعد الأمان

لا تعتمد اللعبة على العميل لحساب الضرر أو الموارد أو نتيجة المباراة. يجب أن يكون الخادم هو صاحب القرار في الصحة والذخيرة والموارد والتحالفات وحالة المنطقة والنتيجة. يجب التحقق من كل RPC على الخادم، وتحديد معدل الطلبات، ورفض القيم خارج الحدود، وتسجيل الأحداث الحساسة دون تخزين أسرار أو بيانات شخصية غير لازمة.

لا تُحفظ مفاتيح Unity Gaming Services أو مفاتيح قواعد البيانات داخل `Assets` أو `ProjectSettings` أو APK. تستخدم بيئات CI وبيئات الخادم أسرارًا منفصلة، وتُدوّر المفاتيح عند الاشتباه بتسريبها. لا يستخدم عنوان `127.0.0.1` في إصدار الإنتاج؛ يجب توفير عنوان أو Relay من إعدادات بيئة آمنة.

## مسار التطوير دون فتح Unity

يمكن تنفيذ الفحص والبناء من GitHub Actions باستخدام `game-ci/unity-builder`. أما إنشاء مشروع UGS وربط Lobby/Relay وAuthentication فيحتاج حساب Unity وخدمة مفعلة وتكوينًا في لوحة Unity. لا يجوز اعتبار `StartHost()` و`StartClient()` وحدهما خادمًا عالميًا جاهزًا؛ يلزم تشغيل Dedicated Server واختبار الانضمام والانقطاع وإعادة الاتصال وامتلاء الغرفة.

## معيار الجاهزية

لا يُعلن الإصدار متعدد اللاعبين جاهزًا إلا بعد اختبار مباراة محلية بين Host وClient، ثم اختبار Dedicated Server، ثم اختبار Relay أو عنوان عام، ثم اختبار Android على شبكة هاتف فعلية. يجب توثيق زمن الاستجابة والانقطاع وإعادة الاتصال، والتحقق من أن جميع نتائج المباراة تصدر من الخادم.
